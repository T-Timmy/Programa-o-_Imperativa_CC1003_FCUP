#include <stdio.h>

int ocorre (int vec[], int size, int val) {

  for (int i=0; i<size; i++){

    if(vec[i]==val){
      return 1;
    }

  }
  return 0;
}


int main (void) {

  int v[] = {1, 3, 5, 8, 11};

  printf("%d\n", ocorre(v, 5, 8));

  return 0;
}
