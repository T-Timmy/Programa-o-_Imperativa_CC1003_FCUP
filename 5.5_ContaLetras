# include <stdio.h>
# include <ctype.h>


int main (void) {

  int ch;
  int conta = 0;

  while((ch = getchar() )!= '\n'){
    if (isalpha(ch)) {
      conta++;
    }
  }

  printf("Tem %d letras\n", conta);
}
