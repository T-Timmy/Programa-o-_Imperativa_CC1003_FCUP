#include <stdio.h>

int main (void) {

int a, b, c ;

printf("Introduza os três valores:"); scanf("%d , %d, %d", &a, &b, &c );

if ( a>=b && a>=c ) {

  printf("O maior valor é : %d\n", a );

} else if (b>=a && b>=c) {

  printf("O maior valor é : %d\n", b );

} else {

  printf("O maior valor é : %d\n", c );

}

  return 0;
}
